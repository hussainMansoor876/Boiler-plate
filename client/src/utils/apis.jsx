const authApi = `/api/user`
const getApi = `/api/get`
const postApi = `/api/post`

const AUTH = {
    LOGIN: `${authApi}/login`,
    SIGNUP: `${authApi}/signup`
}

const GET = {
}

const POST = {
}

export {
    AUTH,
    GET,
    POST
}